# 비밀·로컬 데이터는 절대 커밋하지 않음
.env
**/config.json

# 백업 폴더(읽기 전용 보존물·토큰 포함) — 커밋 금지
backup_*/

# 봇 토큰·API 키가 들어있는 설정(커밋 금지) — *.example 만 커밋
reporting/nk_config.json
reporting/genz_config.json
reporting/gyaru_config.json
reporting/llm_config.json
reporting/channels.json
reporting/memory_*.json

# 빌드·런타임 아티팩트
volumes/
venv/
.venv/
.venv_*/
__pycache__/
__pycache_old__/
*.pyc
*.bak
.ruff_cache/
hub/hub.db
