# 사내 보고 멀티에이전트 — 런타임 의존성
# 두뇌: Nous Research AIAgent 프레임워크(외부 pip) 위에서 구동, 모델은 DeepSeek V4 Flash(OpenRouter).
# 통로: Mattermost(WebSocket 수신 + REST 게시) = 메시징 게이트웨이.
# Python 3.12 이상 기준(하한 3.12). 패키지 버전은 모두 하한(>=)만 두어 최신 파이썬에서도
# 가용한 wheel 로 설치된다. 운영 ARM64(DGX) 환경에서는 cp312 wheel 이 검증돼 있다.

hermes-agent==0.17.0   # Nous Research 외부 pip 패키지(변경 불가) — AIAgent 런타임
websockets>=15.0       # Mattermost WebSocket 게이트웨이 수신

# ── Vault RAG(로컬 전용, 네트워크·API 키 0) ───────────────────────────────────
# 핵심 검색(FTS5)·writer·마이그레이션·MOC 동기화는 표준 라이브러리(sqlite3)만으로 동작한다.
# 아래 임베딩 패키지는 하이브리드(어휘 FTS5 + 의미) 검색을 켜는 정식 의존성이다. 모델은
# 최초 1회만 로컬 캐시(all-MiniLM-L6-v2, 384dim)되며 이후 HF_HUB_OFFLINE 로 100% 오프라인
# 추론(네트워크·API 키 사용 0). 패키지 미설치 시에도 코드는 에러 없이 FTS5 단독으로 graceful
# degrade 하므로, 임베딩 추론을 끄려면 단순히 이 줄을 제거하면 된다.
sentence-transformers>=2.7  # 로컬 임베딩 all-MiniLM-L6-v2 — 하이브리드 의미검색(torch/transformers 포함).
# numpy: 의미검색 코사인을 행렬 연산으로 벡터화(대량 노트에서 순수파이썬 대비 수십~수백배 빠름).
# 부재 시 순수파이썬 코사인으로 graceful degrade(정확도 동일). sentence-transformers 가 이미
# 끌어오지만 vault_rag 가 직접 import 하므로 명시한다.
numpy>=1.24
