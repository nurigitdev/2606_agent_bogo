# 사내 보고 멀티에이전트 — 런타임 의존성
# 두뇌: Hermes Agent(Nous Research) 프레임워크 위에서 구동, 모델은 DeepSeek V4 Flash(OpenRouter).
# 통로: Mattermost(WebSocket 수신 + REST 게시) = 메시징 게이트웨이.
# Python 3.12 기준(3.14는 일부 의존성 wheel 미제공으로 venv는 3.12 사용).

hermes-agent==0.17.0   # Hermes Agent 런타임(AIAgent, provider 레이어, 도구셋)
websockets>=15.0       # Mattermost WebSocket 게이트웨이 수신

# ── Vault RAG(로컬 전용, 네트워크·API 키 0) ───────────────────────────────────
# 핵심 검색(FTS5)·writer·마이그레이션·MOC 동기화는 표준 라이브러리(sqlite3)만으로 동작한다.
# 아래 임베딩 패키지는 '의미검색 보강용 선택 의존성'이다 — 미설치 시 FTS5 단독으로 graceful
# degrade 한다(에러 아님). 설치하면 하이브리드(어휘+의미) 검색이 켜진다. 모델은 최초 1회만
# 로컬 캐시되며 이후 HF_HUB_OFFLINE 로 오프라인 동작(네트워크·키 사용 0).
sentence-transformers>=2.7  # (선택) 로컬 임베딩 all-MiniLM-L6-v2 — 의미검색 보강. 없으면 FTS5-only.
